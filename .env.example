# Kvetch Configuration

# Multi-user mode toggle (set to 'true' to enable remote sync and authentication)
VITE_MULTI_USER_MODE=true

# Remote sync configuration (only used if VITE_MULTI_USER_MODE=true)
VITE_SYNC_GATEWAY_URL=http://localhost:4984
VITE_DATABASE_NAME=kvetch-shared
VITE_ADMIN_API_URL=http://localhost:4985

# Instance configuration (for multi-tenant setups)
# VITE_INSTANCE_ID=demo
# VITE_INSTANCE_NAME=Demo

# Feature flags (only apply in multi-user mode)
VITE_ENABLE_USER_MANAGEMENT=true
VITE_ENABLE_STAFF_ACCOUNTS=true
VITE_ENABLE_INSTANCE_SELECTION=false

# App metadata
VITE_APP_VERSION=1.0.0

# Development vs Production Notes:
# - For single-user offline-only mode: Set VITE_MULTI_USER_MODE=false
# - For multi-user mode with local backend: Set VITE_MULTI_USER_MODE=true with localhost URLs
# - For production deployment: Use production URLs and deploy config.json instead of .env